<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Search</title>
</head>
<body>
	<h1>Search Results: #<%= search %></h1>
	<ul>
		<% tagged.each do |tag| %>
			<% tag.each do |post| %>
				<li><a href="<%= post.url %>"><%= post.content %></a></li>
			<% end %>
		<% end %>
	</ul>
</body>
</html>